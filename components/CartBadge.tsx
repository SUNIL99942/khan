'use client';import {useEffect,useState} from 'react';export function CartBadge(){const [n,setN]=useState(0);useEffect(()=>{const f=()=>{try{setN((JSON.parse(localStorage.getItem('atoz_cart')||'[]') as any[]).reduce((a,x)=>a+x.quantity,0))}catch{}};f();window.addEventListener('storage',f);return()=>window.removeEventListener('storage',f)},[]);return <a href="/cart">🛒{n>0?<sup>{n}</sup>:''}</a>}
