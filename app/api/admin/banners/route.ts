import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';export async function POST(req:Request){try{const u=await requireAdmin();const d=await req.json();const b=await prisma.banner.create({data:{title:d.title,imageUrl:d.imageUrl,mobileImageUrl:d.mobileImageUrl,href:d.href,slot:d.slot||'hero',sortOrder:Number(d.sortOrder||0),startsAt:d.startsAt?new Date(d.startsAt):null,endsAt:d.endsAt?new Date(d.endsAt):null,active:d.active!==false}});await prisma.auditLog.create({data:{userId:u!.id,action:'CREATE',entity:'Banner',entityId:b.id}});return Response.json({banner:b},{status:201})}catch(e:any){return Response.json({error:e.message||'Could not create banner'},{status:400})}}
