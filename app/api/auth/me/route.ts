import {getSession} from '@/lib/auth';import {prisma} from '@/lib/prisma';export async function GET(){const s=await getSession();if(!s)return Response.json({user:null},{status:401});const user=await prisma.user.findUnique({where:{id:s.id},include:{addresses:true}});return Response.json({user})}
