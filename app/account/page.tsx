import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {AddressBook} from '@/components/AddressBook';export default async function Account(){const s=await getSession();if(!s)redirect('/login');return <main className="page"><div className="container"><h1>My Account</h1><div className="grid two"><div className="panel"><h2>Profile & privacy</h2><p>Secure account, saved delivery locations, orders and wishlist.</p><a className="primary" href="/orders">View orders</a></div><div className="panel"><h2>Quick links</h2><a href="/products">Continue shopping →</a><br/><a href="/cart">Cart →</a></div></div><div style={{marginTop:18}}><AddressBook/></div></div></main>}
