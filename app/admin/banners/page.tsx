import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {redirect} from 'next/navigation';export default async function AdminBanners(){try{await requireAdmin()}catch{redirect('/login')}const bs=await prisma.banner.findMany({orderBy:{sortOrder:'asc'}});return <main className="admin"><aside className="adminSide"><div className="brand">🐼 Atoz</div><a href="/admin">Dashboard</a><a href="/admin/products">Products</a><a href="/admin/categories">Categories</a><a href="/admin/banners">Banners</a></aside><section className="adminMain"><h1>Banners & Posters</h1><div className="panel"><p>JPG, PNG and WebP assets can be stored via the upload API. Slots support hero, promo, category and campaign placements with scheduling and ordering.</p><table className="table"><thead><tr><th>Title</th><th>Slot</th><th>Order</th><th>Active</th></tr></thead><tbody>{bs.map(b=><tr key={b.id}><td>{b.title}</td><td>{b.slot}</td><td>{b.sortOrder}</td><td>{b.active?'Yes':'No'}</td></tr>)}</tbody></table></div></section></main>}
