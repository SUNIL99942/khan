import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {redirect} from 'next/navigation';export default async function AdminOrders(){try{await requireAdmin()}catch{redirect('/login')}const os=await prisma.order.findMany({include:{user:true},orderBy:{createdAt:'desc'},take:100});return <main className="admin"><aside className="adminSide"><div className="brand">🐼 Atoz</div><a href="/admin">Dashboard</a><a href="/admin/products">Products</a><a href="/admin/orders">Orders</a></aside><section className="adminMain"><h1>Orders</h1><div className="panel"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Status</th></tr></thead><tbody>{os.map(o=><tr key={o.id}><td>{o.orderNumber}</td><td>{o.user.name}</td><td>₹{(o.total/100).toFixed(0)}</td><td><span className="pill">{o.status}</span></td></tr>)}</tbody></table></div></section></main>}
