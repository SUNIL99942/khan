import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {redirect} from 'next/navigation';export default async function Settings(){try{await requireAdmin()}catch{redirect('/login')}const s=await prisma.siteSetting.findMany();return <main className="admin"><aside className="adminSide"><div className="brand">🐼 Atoz</div><a href="/admin">Dashboard</a><a href="/admin/settings">Settings</a></aside><section className="adminMain"><h1>Site Settings</h1><div className="panel"><p>Global settings are stored in the database so branding, homepage content, contact information, policies and feature flags can be changed without source-code edits.</p>{s.map(x=><div key={x.id} style={{padding:'10px 0',borderBottom:'1px solid #eee'}}><b>{x.key}</b><pre>{JSON.stringify(x.value,null,2)}</pre></div>)}</div></section></main>}
