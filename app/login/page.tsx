'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/account');else setErr((await x.json()).error||'Login failed')}return <main className="page"><div className="container" style={{maxWidth:520}}><form className="panel form" onSubmit={go}><h1>Welcome back</h1><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="primary">Login</button>{err&&<div className="notice">{err}</div>}<a href="/register">New customer? Create account →</a></form></div></main>}
