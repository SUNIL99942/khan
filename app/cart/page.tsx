'use client';import {useEffect,useState} from 'react';import {money} from '@/lib/format';export default function Cart(){const [cart,setCart]=useState<any[]>([]);useEffect(()=>setCart(JSON.parse(localStorage.getItem('atoz_cart')||'[]')),[]);function save(x:any[]){setCart(x);localStorage.setItem('atoz_cart',JSON.stringify(x))}const total=cart.reduce((a,x)=>a+x.price*x.quantity,0);return <main className="page"><div className="container"><h1>Your Cart</h1>{!cart.length?<div className="panel">Your cart is empty. <a href="/products">Shop now →</a></div>:<div className="grid two"><div className="panel">{cart.map((x,i)=><div key={i} style={{display:'flex',gap:12,padding:'14px 0',borderBottom:'1px solid #eee'}}><img src={x.image} style={{width:90,height:90,objectFit:'cover',borderRadius:12}}/><div style={{flex:1}}><b>{x.name}</b><p>{money(x.price)}</p><button className="secondary" onClick={()=>{const c=[...cart];c[i].quantity=Math.max(1,c[i].quantity-1);save(c)}}>−</button> <b>{x.quantity}</b> <button className="secondary" onClick={()=>{const c=[...cart];c[i].quantity++;save(c)}}>+</button> <button className="secondary" onClick={()=>save(cart.filter((_,j)=>j!==i))}>Remove</button></div></div>)}</div><div className="panel"><h2>Summary</h2><p>Subtotal: <b>{money(total)}</b></p><p>Shipping: {total>=49900?'FREE':money(4900)}</p><h2>Total: {money(total+(total>=49900?0:4900))}</h2><a className="primary" style={{display:'block',textAlign:'center'}} href="/checkout">Proceed to Checkout</a></div></div>}</div></main>}
