import {getSession} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {redirect,notFound} from 'next/navigation';export default async function OrderDetail({params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)redirect('/login');const {id}=await params;const o=await prisma.order.findFirst({where:{id,userId:s.id},include:{items:true,events:{orderBy:{createdAt:'asc'}},shipment:true}});if(!o)notFound();return <main className="page"><div className="container"><div className="sectionHead"><h1>Order {o.orderNumber}</h1><span className="pill">{o.status}</span></div><div className="grid two"><div className="panel"><h2>Tracking timeline</h2>{o.events.map(e=><div key={e.id} style={{padding:'12px 0',borderBottom:'1px solid #eee'}}>🟢 <b>{e.status}</b><br/><small>{e.message} · {e.createdAt.toLocaleString('en-IN')}</small></div>)}</div><div className="panel"><h2>Items</h2>{o.items.map(i=><div key={i.id} style={{display:'flex',gap:12,padding:'10px 0',borderBottom:'1px solid #eee'}}><img src={i.imageUrl||'/demo/placeholder.svg'} style={{width:60,height:60,objectFit:'cover',borderRadius:10}}/><span>{i.name}<br/>Qty {i.quantity} · ₹{(i.unitPrice/100).toFixed(0)}</span></div>)}<h3>Total ₹{(o.total/100).toFixed(0)}</h3></div></div></div></main>}
