import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {prisma} from '@/lib/prisma';
export default async function Orders(){const s=await getSession();if(!s)redirect('/login');const orders=await prisma.order.findMany({where:{userId:s.id},orderBy:{createdAt:'desc'},take:50});return <main className="page"><div className="container"><h1>Orders & Tracking</h1>{orders.length===0?<div className="panel">No orders yet. <a href="/products">Start shopping →</a></div>:<div className="grid">{orders.map(o=><div className="panel" key={o.id}><div style={{display:'flex',justifyContent:'space-between'}}><b>{o.orderNumber}</b><span className="pill">{o.status}</span></div><p>Total ₹{(o.total/100).toFixed(0)} · {o.paymentStatus}</p><a href={'/orders/'+o.id}>Track order →</a></div>)}</div>}</div></main>}
