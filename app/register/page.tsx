'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Register(){const [f,setF]=useState({name:'',email:'',phone:'',password:''});const [err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();const x=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});if(x.ok)r.push('/account');else setErr((await x.json()).error||'Registration failed')}return <main className="page"><div className="container" style={{maxWidth:520}}><form className="panel form" onSubmit={go}><h1>Create your Atoz account</h1>{Object.entries(f).map(([k,v])=><input key={k} type={k==='password'?'password':k==='email'?'email':'text'} placeholder={k[0].toUpperCase()+k.slice(1)} value={v} onChange={e=>setF({...f,[k]:e.target.value})} required={k!=='phone'}/>)}<button className="primary">Create account</button>{err&&<div className="notice">{err}</div>}</form></div></main>}
